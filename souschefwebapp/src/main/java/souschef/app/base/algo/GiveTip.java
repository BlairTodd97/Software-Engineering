package souschef.app.base.algo;

// this algorithm will consider tips that the user has already received and give a new tip
public class GiveTip {

    public void pickTip() {
        // step 1 - consider all tips
        // step 2 - consider tips recently sent to user [recent 5]
        // step 3 - consider recent searched from user
        // step 4 - find all tips similar to what user would prefer
        // step 5 - filter tips already sent to user
        // step 6 - random pick any from the top 5 results
    }
}
